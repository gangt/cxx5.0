package com.xi6666.app.di.componets;

import com.xi6666.addoil.model.UseCoponModel;
import com.xi6666.addoil.view.AddOilPayResultAct;
import com.xi6666.app.di.scope.ModelScope;
import com.xi6666.cardbag.model.CouponModel;
import com.xi6666.cardbag.model.IllegaBagModel;
import com.xi6666.cardbag.view.mvp.OilRechargeDetailsModel;

import com.xi6666.common.BuriedPoint;
import com.xi6666.illegal.other.IllegaHomeModel;
import com.xi6666.illegal.other.IllegaSurePayModel;
import com.xi6666.seckill.model.SecKillModel;
import com.xi6666.view.dialog.PromotionDialogAct;
import com.xi6666.view.dialog.PromotionDialogFrg;

import dagger.Component;

/**
 * Created by Mr_yang on 2017/1/16.
 */

@ModelScope
@Component(dependencies = AppComponets.class)
public interface ApiComponent {
    void Inject(SecKillModel secKillModel);

    void Inject(CouponModel couponModel);

    void Inject(BuriedPoint buriedPoint);

    void Inject(IllegaBagModel illegaBagModel);

    void Inject(IllegaHomeModel illegaHomeModel);

    void Inject(IllegaSurePayModel illegaSurePayModel);

    void Inject(UseCoponModel useCoponModel);

    void Inject(PromotionDialogFrg useCoponModel);

    void Inject(PromotionDialogAct useCoponModel);

    void Inject(AddOilPayResultAct useCoponModel);

    void Inject(OilRechargeDetailsModel oilRechargeDetailsModel);


}
